{
  "name": "accounting-gemma",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "wrangler deploy --dry-run",
    "check:staging": "wrangler deploy --dry-run --env staging",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.133.0"
  }
}
